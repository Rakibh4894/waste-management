git install
git config --global user.name "test name";
git config --global user.email "kamrul@gmail.com"

install nodejs 
install xampp
install composer and path to xampp/php/php.exe
go to htdocs

open command prompt (CMD)
git clone https://github.com/Rakibh4894/waste-management.git
firsttime will ask for 

cd waste-management
composer install


php artisan serve
npm run dev

